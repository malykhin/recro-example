{
  "name": "recro-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "lodash": "^4.17.15",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.4.1",
    "recro": "^0.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:all": "npm run start --prefix ./micros/vanilla-app & npm run start --prefix ./micros/react-app & npm run start",
    "install:all": "npm install --prefix ./micros/react-app & npm install --prefix ./micros/vanilla-app & npm install"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
